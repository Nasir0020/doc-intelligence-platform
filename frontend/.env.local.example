# Copy to .env.local for local development.
# Must be prefixed NEXT_PUBLIC_ to be readable in browser-side code —
# see the comment in src/lib/api.ts for why.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
